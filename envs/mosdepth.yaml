# Upstream nf-core/sarek module env (conda equivalent of the wave.seqera.io
# community image). Channel NAMES only - CN users point conda at a
# domestic mirror, international users use the defaults.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # bioconda has pruned the upstream 0.3.9 pin (old builds dropped from
  # the channel — live: PackagesNotFoundInChannelsError)
  - mosdepth>=0.3.9,<0.4
  - htslib
